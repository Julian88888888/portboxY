# Исправление загрузки изображений в Vercel

## Проблема

При загрузке изображения в альбом возникала ошибка:
```
Failed to execute 'json' on 'Response': Unexpected end of JSON input
Status Code: (пусто)
```

Это означало, что serverless function для `/api/albums/:id/images` не существовал или возвращал пустой ответ.

## Решение

Создан serverless function `api/albums/[id]/images.js` для обработки загрузки изображений.

### Что было сделано:

1. ✅ **Создан serverless function** `api/albums/[id]/images.js`:
   - Обрабатывает `POST /api/albums/:id/images`
   - Использует `busboy` для парсинга `multipart/form-data`
   - Загружает изображения напрямую в Supabase Storage
   - Сохраняет запись в таблице `images`

2. ✅ **Добавлена зависимость** `busboy` в `package.json`:
   - Необходима для обработки multipart/form-data в Vercel

3. ✅ **Обработка файлов**:
   - Валидация типа файла (только изображения)
   - Валидация размера (максимум 10MB)
   - Загрузка в Supabase Storage bucket `profile-photos`
   - Автоматическое создание уникального имени файла

## Что нужно сделать

### 1. Установите зависимость busboy

```bash
npm install busboy
```

Или если используете pnpm:
```bash
pnpm add busboy
```

### 2. Задеплойте на Vercel

```bash
git add .
git commit -m "Add image upload serverless function"
git push
```

### 3. Убедитесь, что переменные окружения установлены

В Vercel Dashboard → Settings → Environment Variables:
- `REACT_APP_SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY` (или `REACT_APP_SUPABASE_ANON_KEY`)

## Проверка

После деплоя:

1. **Попробуйте загрузить изображение в альбом:**
   - Откройте Dashboard → Portfolio
   - Создайте альбом (если еще не создан)
   - Нажмите "📷 Add Images"
   - Выберите изображение и загрузите

2. **Проверьте в консоли браузера:**
   - Не должно быть ошибок "Failed to execute 'json'"
   - Запрос должен вернуть статус 201 Created
   - В ответе должен быть объект с данными изображения

3. **Проверьте в Supabase:**
   - Storage → profile-photos bucket
   - Должны появиться файлы в папке `albums/`
   - Таблица `images` должна содержать новую запись

## Структура файлов

```
project/
├── api/
│   └── albums/
│       ├── index.js              # GET и POST /api/albums
│       └── [id]/
│           └── images.js         # POST /api/albums/:id/images
├── package.json                  # С зависимостью busboy
└── vercel.json                   # Конфигурация Vercel
```

## Troubleshooting

### Ошибка "Module not found: busboy"
- Убедитесь, что `busboy` установлен: `npm install busboy`
- Перезапустите deployment в Vercel

### Ошибка "No image file found"
- Убедитесь, что в FormData поле называется `image`
- Проверьте, что Content-Type заголовок установлен правильно

### Ошибка "Supabase not configured"
- Проверьте переменные окружения в Vercel
- Убедитесь, что они добавлены для всех окружений

### Изображение не загружается в Storage
- Проверьте права доступа к bucket `profile-photos` в Supabase
- Убедитесь, что bucket существует и публичен

## Важные замечания

⚠️ **Размер файла**: Максимальный размер изображения - 10MB

⚠️ **Типы файлов**: Разрешены только: JPG, PNG, GIF, WebP

⚠️ **Bucket**: Используется существующий bucket `profile-photos`. Если нужен отдельный bucket для альбомов, создайте его в Supabase и обновите код.

