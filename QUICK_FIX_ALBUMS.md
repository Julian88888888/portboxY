# Быстрое исправление ERR_CONNECTION_RESET

## Проблема
`POST http://localhost:5002/api/albums net::ERR_CONNECTION_RESET`

## Быстрое решение

### 1. Убедитесь, что backend запущен

```bash
cd backend
npm start
```

Должно появиться:
```
🚀 Server running on port 5002
🖼️  Albums API: http://localhost:5002/api/albums
```

### 2. Проверьте, что сервер работает

Откройте в браузере:
```
http://localhost:5002/health
```

Или через curl:
```bash
curl http://localhost:5002/health
```

### 3. Проверьте тестовый endpoint

```bash
curl http://localhost:5002/api/albums/test
```

Должен вернуть: `{"success":true,"message":"Albums API is working!"}`

### 4. Если сервер не запускается

**Ошибка: MongoDB connection failed**
- Это нормально! Albums API использует Supabase, не MongoDB
- Сервер должен продолжить работу несмотря на эту ошибку

**Ошибка: Cannot find module**
```bash
cd backend
npm install
```

### 5. Проверьте переменные окружения

Создайте `backend/.env`:
```env
PORT=5002
SUPABASE_URL=ваш_supabase_url
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
```

### 6. Если всё ещё не работает

Временно отключите аутентификацию для теста:

В `backend/routes/albums.js` замените:
```javascript
router.post('/', authenticateToken, albumController.createAlbum);
```

На:
```javascript
router.post('/', albumController.createAlbum); // Временно без auth
```

**ВАЖНО**: Верните аутентификацию после тестирования!

### 7. Перезапустите всё

```bash
# Остановите backend (Ctrl+C)
# Запустите снова
cd backend
npm start

# В другом терминале перезапустите frontend
npm start
```

## Что было исправлено

1. ✅ Удалён дубликат `module.exports` в routes/albums.js
2. ✅ MongoDB больше не обязателен (albums используют Supabase)
3. ✅ Улучшена обработка ошибок
4. ✅ Добавлен тестовый endpoint `/api/albums/test`
5. ✅ Улучшены сообщения об ошибках

## Проверка после исправления

1. Backend запущен на порту 5002
2. Health endpoint работает: `http://localhost:5002/health`
3. Test endpoint работает: `http://localhost:5002/api/albums/test`
4. Frontend может подключиться к backend

Если всё это работает, но создание альбома всё ещё не работает - проблема в аутентификации или Supabase конфигурации.



