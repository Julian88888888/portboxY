# Интеграция Profile Settings в Dashboard

## ✅ Что было сделано

1. **Создан компонент ProfileSettings.tsx** с функциональностью:
   - Загрузка фото профиля
   - Загрузка фото заголовка
   - Toggle для показа/скрытия фото профиля
   - Toggle для показа/скрытия фото заголовка

2. **Интегрирован в Dashboard.js**:
   - Компонент добавлен в Tab 1 (Profile Settings)
   - Заменена статичная форма на рабочий компонент

3. **Настроен Backend**:
   - `GET /api/me/profile` - получение настроек профиля
   - `PUT /api/me/profile` - обновление настроек
   - `POST /api/me/profile/upload-url` - получение signed URL для загрузки

## 📋 Шаги для запуска

### 1. Установите зависимости

```bash
# Frontend
npm install @tanstack/react-query typescript @types/react @types/react-dom

# Backend
cd backend
npm install @supabase/supabase-js
```

### 2. Настройте базу данных

Выполните SQL скрипт в Supabase:

```sql
-- Файл: supabase_profiles_setup.sql
-- Создаст таблицу profiles с RLS политиками
```

### 3. Настройте Storage Buckets

Следуйте инструкциям в `supabase_storage_setup.md`:
- Создайте bucket `profile-photos`
- Создайте bucket `profile-headers`
- Настройте storage policies

### 4. Настройте Backend Environment Variables

В `backend/.env`:

```env
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

### 5. Запустите серверы

```bash
# Backend
cd backend
npm run dev

# Frontend
npm start
```

## 🎯 Использование

1. Войдите в систему
2. Перейдите в Dashboard → Tab 1 (Profile)
3. Вы увидите секцию "Profile Settings" с:
   - **Add Profile Photo/Logo** - кнопка загрузки фото профиля
   - **Show Profile Photo/Logo** - переключатель видимости
   - **Add Profile Header Photo** - кнопка загрузки фото заголовка
   - **Show Profile Header Photo** - переключатель видимости

## 🔧 Как это работает

### Загрузка фото:

1. Пользователь выбирает файл (jpg/png/webp, max 5MB)
2. Frontend валидирует файл
3. Frontend запрашивает signed URL у backend: `POST /api/me/profile/upload-url`
4. Backend генерирует signed URL и возвращает путь
5. Frontend загружает файл напрямую в Supabase Storage
6. Frontend сохраняет путь в БД: `PUT /api/me/profile`

### Toggle видимости:

1. Пользователь переключает toggle
2. Frontend сразу обновляет настройки: `PUT /api/me/profile`
3. Изменения сохраняются в БД
4. При отображении публичного профиля проверяется `show_profile_photo` и `show_profile_header`

## 🐛 Troubleshooting

### Фото не загружается

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что backend запущен
3. Проверьте, что SUPABASE_URL и SUPABASE_SERVICE_ROLE_KEY настроены
4. Проверьте, что storage buckets созданы

### Toggle не работает

1. Проверьте консоль браузера
2. Убедитесь, что таблица `profiles` создана
3. Проверьте Network tab - должен быть запрос `PUT /api/me/profile`

### Ошибка "Failed to get profile settings"

1. Проверьте, что пользователь авторизован
2. Проверьте, что JWT token передается в заголовке Authorization
3. Проверьте, что таблица `profiles` существует
4. Проверьте RLS политики в Supabase

## 📝 Структура данных

### Таблица profiles:

```sql
id UUID PRIMARY KEY (references auth.users)
profile_photo_path TEXT
profile_header_path TEXT
show_profile_photo BOOLEAN DEFAULT true
show_profile_header BOOLEAN DEFAULT true
updated_at TIMESTAMPTZ
```

### Storage структура:

```
profile-photos/
  {userId}/
    profile_photo/
      {timestamp}-{filename}

profile-headers/
  {userId}/
    profile_header/
      {timestamp}-{filename}
```

## ✅ Проверка работы

1. ✅ Загрузите фото профиля - должно появиться превью
2. ✅ Переключите "Show Profile Photo" OFF - фото должно скрыться в публичном профиле
3. ✅ Переключите обратно ON - фото должно появиться
4. ✅ Загрузите фото заголовка - должно появиться превью
5. ✅ Переключите "Show Profile Header" OFF/ON - должно работать

## 🔄 Следующие шаги

После того как всё работает, можно:
- Добавить кроппинг изображений перед загрузкой
- Добавить оптимизацию изображений
- Добавить предпросмотр в публичном профиле
- Добавить удаление старых фото при загрузке новых



