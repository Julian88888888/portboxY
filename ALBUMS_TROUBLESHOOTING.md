# Troubleshooting Albums API - ERR_CONNECTION_RESET

## Проблема: `ERR_CONNECTION_RESET` при создании альбома

Эта ошибка означает, что соединение с backend сервером было разорвано. Вот как это исправить:

## Шаг 1: Проверьте, запущен ли backend сервер

```bash
# Перейдите в папку backend
cd backend

# Убедитесь, что зависимости установлены
npm install

# Запустите сервер
npm start
# или для разработки:
npm run dev
```

Вы должны увидеть:
```
🚀 Server running on port 5002
🖼️  Albums API: http://localhost:5002/api/albums
```

## Шаг 2: Проверьте health endpoint

Откройте в браузере или через curl:
```bash
curl http://localhost:5002/health
```

Должен вернуться JSON:
```json
{
  "success": true,
  "message": "Server is running"
}
```

## Шаг 3: Проверьте переменные окружения

Создайте файл `backend/.env` (если его нет):

```env
PORT=5002
NODE_ENV=development

# Supabase configuration (для albums API)
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
# ИЛИ
REACT_APP_SUPABASE_URL=your_supabase_url
REACT_APP_SUPABASE_ANON_KEY=your_anon_key

# MongoDB (опционально, albums не требуют MongoDB)
MONGODB_URI=mongodb://localhost:27017/model_portfolio_db
```

## Шаг 4: Проверьте консоль backend сервера

Если сервер падает при запуске, проверьте ошибки в консоли:

### Ошибка: "Cannot find module"
```bash
cd backend
npm install
```

### Ошибка: "MongoDB connection failed"
Это нормально! Albums API использует Supabase, а не MongoDB. Сервер должен продолжить работу.

### Ошибка: "Supabase not configured"
Убедитесь, что переменные окружения SUPABASE_URL и SUPABASE_SERVICE_ROLE_KEY установлены.

## Шаг 5: Проверьте CORS

Убедитесь, что ваш frontend URL разрешен в `backend/server.js`:

```javascript
const allowedOrigins = [
  'http://localhost:3000', 
  'http://localhost:3001', 
  // добавьте ваш URL если отличается
];
```

## Шаг 6: Проверьте таблицы в Supabase

Убедитесь, что таблицы `albums` и `images` созданы:

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Выполните:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_name IN ('albums', 'images');
```

Если таблиц нет, выполните `albums_schema_fixed.sql`

## Шаг 7: Тестирование API напрямую

Проверьте, работает ли API без frontend:

```bash
# Создать альбом (замените <token> на ваш Supabase token)
curl -X POST http://localhost:5002/api/albums \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "title": "Test Album",
    "description": "Test"
  }'
```

Если это работает, проблема в frontend или CORS.

## Шаг 8: Проверьте frontend переменные

Убедитесь, что в `.env` (корень проекта) установлено:

```env
REACT_APP_API_URL=http://localhost:5002/api
```

Или frontend будет использовать дефолтный URL.

## Шаг 9: Проверьте браузерную консоль

Откройте DevTools (F12) → Console и Network tabs:

1. **Console**: Проверьте ошибки JavaScript
2. **Network**: 
   - Найдите запрос к `/api/albums`
   - Проверьте статус (должен быть 200 или 201)
   - Проверьте Request Headers (должен быть Authorization)
   - Проверьте Response (если есть)

## Шаг 10: Временное отключение аутентификации (для тестирования)

Если проблема в аутентификации, временно отключите её для тестирования:

В `backend/routes/albums.js`:

```javascript
// Временно убрать authenticateToken для тестирования
router.post('/', /* authenticateToken, */ albumController.createAlbum);
```

**ВАЖНО**: Верните аутентификацию после тестирования!

## Частые причины ошибки

### 1. Backend сервер не запущен
**Решение**: Запустите `npm start` в папке backend

### 2. Неправильный порт
**Решение**: Убедитесь, что backend на порту 5002, а frontend обращается к правильному URL

### 3. CORS блокирует запрос
**Решение**: Добавьте ваш frontend URL в allowedOrigins в server.js

### 4. Аутентификация падает
**Решение**: Проверьте, что Supabase middleware работает корректно

### 5. Таблицы не созданы
**Решение**: Выполните `albums_schema_fixed.sql` в Supabase

### 6. Supabase не настроен
**Решение**: Установите переменные окружения SUPABASE_URL и SUPABASE_SERVICE_ROLE_KEY

## Логи для отладки

Добавьте логирование в `backend/controllers/albumController.js`:

```javascript
console.log('createAlbum called');
console.log('Request body:', req.body);
console.log('Supabase configured:', !!supabase);
```

## Быстрая проверка

Выполните все команды по порядку:

```bash
# 1. Проверьте backend
cd backend
npm start

# 2. В другом терминале проверьте health
curl http://localhost:5002/health

# 3. Проверьте albums endpoint (без auth для теста)
curl http://localhost:5002/api/albums

# 4. Если всё работает, проблема в frontend или аутентификации
```

## Если ничего не помогает

1. Перезапустите backend сервер
2. Очистите кэш браузера (Ctrl+Shift+R)
3. Проверьте firewall/антивирус (может блокировать localhost)
4. Попробуйте другой браузер
5. Проверьте, не запущен ли другой процесс на порту 5002:
   ```bash
   lsof -i :5002  # Mac/Linux
   netstat -ano | findstr :5002  # Windows
   ```



