# Как создать альбом и добавить изображения

## Проблема: Альбомы не отображаются

Если альбомы не показываются, выполните следующие шаги:

## Шаг 1: Убедитесь, что таблицы созданы

1. Откройте Supabase Dashboard: https://app.supabase.com
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Скопируйте и выполните весь SQL из файла `albums_schema_fixed.sql`
5. Проверьте, что таблицы созданы:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_name IN ('albums', 'images');
```

Должны вернуться обе таблицы.

## Шаг 2: Создайте альбом в Dashboard

1. Откройте приложение и войдите
2. Перейдите в **Dashboard** (Панель управления)
3. Откройте вкладку **"Portfolio"** (Tab 2)
4. Прокрутите до секции **"Image Albums (New API)"**
5. Нажмите **"+ Create New Album"**
6. Заполните форму:
   - **Title** (Название) - обязательно
   - **Description** (Описание) - необязательно
   - **Cover Image** (Обложка) - необязательно
7. Нажмите **"Create Album"**

## Шаг 3: Проверьте, что альбом создан

### В консоли браузера (F12 → Console):
Должны появиться логи:
```
Album created with ID: <uuid>
Reload albums result: {success: true, data: [...]}
Loaded albums: 1
```

### Проверьте API напрямую:
```bash
curl http://localhost:5002/api/albums
```

Должен вернуть массив с вашим альбомом.

## Шаг 4: Добавьте изображения в альбом

1. В Dashboard → Portfolio → Image Albums
2. Найдите созданный альбом
3. Нажмите кнопку **"📷 Add Images"**
4. Выберите изображение
5. Нажмите **"Upload Image"**
6. Повторите для добавления нескольких изображений

**Важно:** Первое загруженное изображение автоматически становится обложкой альбома!

## Шаг 5: Просмотрите альбом в ModelPage

1. Откройте ModelPage (публичная страница)
2. Прокрутите до секции **"PORTFOLIO"**
3. Вы должны увидеть альбомы с обложками
4. Кликните на альбом → откроется модальное окно со всеми изображениями

## Отладка

### Если альбом не создаётся:

1. **Проверьте консоль браузера (F12 → Console)**
   - Ищите ошибки (красным цветом)
   - Проверьте логи создания альбома

2. **Проверьте Network (F12 → Network)**
   - Найдите запрос `POST /api/albums`
   - Проверьте статус (должен быть 201)
   - Проверьте Response

3. **Проверьте backend логи:**
   ```bash
   docker-compose logs backend --tail 50
   ```

4. **Проверьте таблицы в Supabase:**
   ```sql
   SELECT * FROM albums ORDER BY created_at DESC LIMIT 5;
   SELECT * FROM images ORDER BY created_at DESC LIMIT 5;
   ```

### Если альбом создан, но не отображается:

1. **Обновите страницу** (F5 или Ctrl+R)
2. **Проверьте консоль браузера** на ошибки загрузки
3. **Проверьте, что API возвращает альбомы:**
   ```bash
   curl http://localhost:5002/api/albums
   ```

## Частые проблемы

### Ошибка 500 при создании альбома
- Проверьте, что таблицы созданы в Supabase
- Проверьте переменные окружения (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
- Проверьте backend логи

### Альбом создан, но не виден в списке
- Обновите страницу
- Проверьте консоль браузера
- Убедитесь, что API возвращает альбомы

### Изображения не загружаются
- Проверьте размер файла (максимум 10MB)
- Проверьте формат (JPG, PNG, GIF, WebP)
- Проверьте backend логи

## Готово! 🎉

После выполнения всех шагов:
- ✅ Альбомы создаются в Dashboard
- ✅ Изображения загружаются в альбомы
- ✅ Альбомы отображаются в ModelPage
- ✅ При клике открывается модальное окно с изображениями

